[build]
builder = "DOCKERFILE"
dockerfilePath = "services/auth-service/Dockerfile"
watchPatterns = ["packages/**", "services/auth-service/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
